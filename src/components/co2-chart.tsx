function groupByMinute(data: any[]) {
    const groupedData = {};

    data.forEach((item) => {
        const timeInSeconds = parseFloat(item.time);
    });
}

export default function Co2Chart() {
    return <div>Co2Chart</div>;
}
